name: deployment to server
run-name: 🚀 ${{ github.event.head_commit.message }} deployment triggered by ${{ github.actor }} 🚀
on:
  push:
    branches:
    - main
jobs:
  deploy-stage:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - run: echo "Preparing deploy-stage environment 🔧"
      - name: Prepare ssh tunnel to the server for the deployment
        uses: kielabokkie/ssh-key-and-known-hosts-action@v1
        with:
          ssh-private-key: ${{ secrets.SSH_GITHUB_ACTION }}
          ssh-host: ${{ env.SERVER_IP }}
      - name: Pull repository
        run: ssh root@${{ env.SERVER_IP }} "cd /app && git pull ${{ github.repositoryUrl }}"
      - name: 
        run: ssh root@${{ env.SERVER_IP }} "cd /app && npm install && npm run build"
        # restart http-server
      
